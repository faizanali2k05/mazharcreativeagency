TEAM PHOTOS
===========

Drop the team member headshots here with these EXACT names:

  faizan.jpg   -> Faizan Ali  (the navy 3-piece-suit / blue-tie photo)
  ahmad.jpg    -> Ahmad Raza  (the black-suit / black-tie photo)

They are rendered as circular avatars in src/components/Team.tsx
(cropped to a circle, top-aligned). Square-ish or portrait headshots
work best. Until a file exists, the member's initials are shown instead.

If you save them as .png instead of .jpg, update the `photo` paths in
src/components/Team.tsx (MEMBER_META) to match.
